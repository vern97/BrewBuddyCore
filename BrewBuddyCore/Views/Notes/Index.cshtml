@model IEnumerable<BrewBuddyCore.Models.Note>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateLogged)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoteDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brew)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateLogged)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NoteDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brew.BrewID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NoteID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NoteID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NoteID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
